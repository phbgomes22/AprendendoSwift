//
//  Tarefas.swift
//  ContadorDia1
//
//  Created by Pedro Gomes on 07/10/21.
//

import Foundation



/*
 
 Tarefas a serem feitas:
 
    * Checar se cumpriu meta
        - usar variável computada
        - usar UIColor (218, 243, 255)
            . digital color meter
 
    * diferentes visualizações
        - deixar o usuário ver o total em quantidade de copos, mls ou litros
        - criar enum para a unidade
        - criar struct para o consumo, tendo os valores e funcoes para conversão
    
    *  Meta não será dada pelo usuário, mas por nós
        - struct para meta (ml, copos, dificuldade)
        - enum dificuldade
        - static var ml por copos
        - trazer checar meta para essa struct
        - criar vetor com várias metas diferentes
 
    * Registro de água
        - criar struct para o registro (mls e data)
        - pegar data de agora
        - apresentar na interface quando foi a última vez que bebemos agua
 
 
 */
